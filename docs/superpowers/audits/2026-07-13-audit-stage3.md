# Codebase audit, stage 3 (periphery: cli, supervisor, hotkeys/keymap, transport/hooks/ducking)

Date: 2026-07-13. Method: 4 parallel reviewers + adversarial verification (14 verified, 0 refuted; 11 non-minor over the verification cap; 21 minors). Companion to 2026-07-13-audit-stage1-2.md.

Note: the confirmed cli.py:637 Ctrl+C-revert finding was independently fixed in audit wave 2 (#21) before this report was compiled.

## Confirmed findings (adversarially verified)

### [CRITICAL] src/sonara/cli.py:424 — sonara install guts ~/.sonara/app while the daemon/supervisor is running: _copy_app rmtree+copytree runs at step 2, but the only (best-effort, async) stop is schtasks /end inside sup.install at step 5.

install() calls _copy_app (cli.py:499-507, rmtree at 423-425) BEFORE supervisor.install() issues `schtasks /end` (platform/windows/supervisor.py:851). Worse, the scheduled task's work_dir is APP_DIR/sonara/platform/windows (supervisor.py:103), so the live supervisor's cwd locks that directory on Windows: rmtree deletes bottom-up, removes nearly every file, then raises on the locked dir. The OSError is caught at cli.py:503 and install returns 1, leaving a gutted app copy the respawn loop can no longer run from. A lazily-started daemon (launch_spec spawns pythonw directly, supervisor.py:737-762) is not stopped by /end at all. This exact failure is documented in the user's runbook memory ('stop it before redeploying or copytree leaves platform/windows empty'). Doctor cannot catch it: 'plugin path resolved' only checks sonara/__init__.py exists (cli.py:324-326).

**Fix:** Stop the daemon before touching APP_DIR: move the task /end (plus killing the daemon process, e.g. via a new shutdown protocol message or taskkill on the recorded pid) to before _copy_app, wait for the socket to stop accepting, and copy to a temp dir + os.replace/rename swap so a failed copy never leaves a half-deleted app.

**Verifier:** Confirmed by reading the code. install() runs _copy_app (rmtree+copytree of APP_DIR/sonara, cli.py:423-425, called at 501) at step 2, while the only stop of the running daemon is a best-effort async `schtasks /end` at supervisor.py:851, invoked from sup.install() at step 5 (cli.py:521) — after the destructive copy. The scheduled task's WorkingDirectory is APP_DIR/sonara/platform/windows (supervisor.py:103/template line 80); supervisor_loop.py never chdirs and its Popen passes no cwd, so both the supervisor and daemon hold that directory as cwd, which on Windows blocks its deletion: rmtree deletes files bottom-up then raises OSError on the locked dir, caught at cli.py:503 with a misleading 'check ~/.sonara writable' message, returning 1 and leaving a gutted app the restart loop can no longer run. A lazily-started daemon (launch_spec spawns pythonw directly, supervisor.py:737-762) is not governed by the task, so /end never stops it. Doctor's 'plugin path resolved' only checks sonara/__init__.py exists (cli.py:320-330), so it can miss the gutted state. The user's runbook memory documents this exact failure occurring in practice. No refutation found: no hidden pre-copy stop, no chdir, no atomic temp+rename swap. Critical severity stands: routine upgrade destroys the live installation with no automatic recovery.

### [IMPORTANT] src/sonara/cli.py:462 — After Kokoro provisioning, _ensure_speech_deps targets the uv venv python with `pip install --user`, which cannot work: uv venvs ship without pip and --user is rejected inside any venv, so PyWinRT can never be auto-installed and install() ends rc=1 with the 'SILENT' warning.

_cmd_voices_install (kokoro) chains into install() (cli.py:641), where _daemon_python() now returns the kokoro venv python (cli.py:356-361). requirements-kokoro.txt installs only kokoro-onnx/onnxruntime/numpy - no winrt-* packages - so _winrt_importable(venv_py) is False, and the bootstrap `[python, -m, pip, install, --user, ...]` (cli.py:462) fails twice over ('No module named pip' in a uv venv; --user is invalid in venvs anyway). Result: every kokoro install ends with 'the SPEECH ENGINE is not installed ... it will be SILENT' and exit 1, the daemon rewired onto a venv where the WinRT system-voice backend (platform/windows/tts.py) cannot import, and a manual hint (cli.py:469-470) that also fails for the same no-pip reason.

**Fix:** Install the winrt-* packages into the venv via `uv pip install --python <venv_py> ...` (or add them to requirements-kokoro.txt), and drop --user when the target interpreter is a venv.

**Verifier:** Confirmed end-to-end in code. _cmd_voices_install (kokoro) calls install() (cli.py:641); _daemon_python returns the kokoro venv python because neural_enabled() derives from the venv's existence and the venv is CPython 3.12 (cli.py:350-361, kokoro_provision.py:22, 72). requirements-kokoro.txt installs only kokoro-onnx/onnxruntime/numpy, so _winrt_importable(venv_py) is False. The bootstrap `[venv_py, -m, pip, install, --user, ...]` (cli.py:462) fails twice over: `uv venv` (no --seed) ships without pip ("No module named pip"), and --user is rejected inside any venv. subprocess.run has no check=True so flow falls through to the failing re-verify, _ensure_speech_deps returns False, and install() prints the SILENT warning and returns 1 (cli.py:544-548), so the "Neural voices ready" message at cli.py:642-643 is also skipped. The manual hint (cli.py:469-470) prints the same no-pip command. No other code path installs _WINRT_PACKAGES, and tests stub _winrt_importable/subprocess.run so nothing catches it. Severity stays important: kokoro speech itself still works (the SILENT warning is misleading, not literally true), but every kokoro install exits 1 with a false alarm, the venv daemon loses the WinRT system-voice fallback and pycaw ducking, and the suggested remedy cannot work. Proposed fix (uv pip install --python <venv_py>, or add winrt-*/pycaw to requirements-kokoro.txt, and drop --user for venv targets) is correct.

### [IMPORTANT] src/sonara/cli.py:658 — sonara voices uninstall rmtree's a venv the running daemon (kokoro) or the resident chatterbox worker is executing from; the resulting PermissionError is uncaught, so the user gets a raw traceback and a half-deleted venv, and the daemon is never rewired.

_cmd_voices_uninstall calls kp.uninstall_kokoro() (cli.py:658) / cbp.uninstall_chatterbox() (cli.py:653) with no try/except and no daemon/worker stop first. After kokoro provisioning the daemon runs on the venv's pythonw (supervisor.py:194-204), which locks Scripts/pythonw.exe on Windows; the chatterbox worker keeps its python.exe alive up to chatterbox_idle_unload_s=600 after last use (config.py:24). shutil.rmtree (kokoro_provision.py:119-123, chatterbox_provision.py:104-108) then raises, main() re-raises the OSError as a traceback (cli.py:710-715), the venv is left partially deleted but still 'provisioned' (python.exe existence check, kokoro_provision.py:21-23 / chatterbox.py:39-41), and install() at cli.py:659 never runs so the daemon stays pointed at the broken venv.

**Fix:** Stop the daemon/worker (or at least catch OSError with an actionable 'stop the daemon and retry' message) before rmtree; rewire the daemon first, delete the venv second.

**Verifier:** Confirmed. _cmd_voices_uninstall (cli.py:653/658) rmtree's the engine venv with no try/except and no daemon/worker stop; the only stop (schtasks /end in sup.install, windows/supervisor.py:851) runs from install() at cli.py:659, i.e. after the rmtree, so it never executes on failure. With kokoro installed the daemon runs on the venv's Scripts\pythonw.exe (daemon_pythonw, windows/supervisor.py:194-204) and autostarts/lazy-starts, so the image file is locked; shutil.rmtree (kokoro_provision.py:119-123, no ignore_errors/onerror) raises PermissionError, which main() (cli.py:708-715) re-raises as a raw traceback since it only translates DaemonNotRunning. The chatterbox path is identical: the resident worker (chatterbox.py:259-284) stays alive up to chatterbox_idle_unload_s=600 (config.py:24) locking the venv python.exe against uninstall_chatterbox's rmtree (chatterbox_provision.py:104-108). Result: half-deleted venv, install() never runs, scheduled task still points at the gutted venv pythonw, daemon broken until a manual 'sonara install'. Severity 'important' stands: guaranteed failure in the default configuration but recoverable with no data loss.

### [IMPORTANT] src/sonara/cli.py:586 — uninstall() never stops the running daemon: it deletes daemon.lock while the daemon still holds the singleton mutex, silently swallows the APP_DIR rmtree failure, then prints success - after a reinstall Sonara is dead until the orphan process is killed.

uninstall() (cli.py:556-604) removes the Task Scheduler task (delete only, no /end - supervisor.py:120-125), deletes LOCK_PATH (cli.py:569-582), and rmtree's APP_DIR with `except OSError: pass` (cli.py:586-591). But the daemon/supervisor process keeps running (hotkeys stay live), its cwd inside APP_DIR makes the rmtree partially fail silently, and it still owns the Global mutex. If the user then reinstalls, ensure_running cannot start a new daemon (mutex held by the orphan running from a gutted app dir) and the deleted lockfile means nothing can reach the old one - Sonara is silently broken until reboot/manual kill. Meanwhile uninstall printed 'Removed Sonara runtime files' unconditionally (cli.py:600-601) even when the app copy removal failed.

**Fix:** Terminate the daemon first (shutdown message, or taskkill by recorded pid), verify the socket is gone, then delete files; report rmtree failures instead of passing.

**Verifier:** Confirmed. uninstall() (cli.py:556-604) never stops the daemon: task_uninstall() only runs `schtasks /delete /f` (supervisor.py:120-125), which does not terminate the running supervisor_loop/daemon (install() by contrast does call `/end` at supervisor.py:851). No other kill path exists: CLI `stop` only clears the speech queue and daemon.py has no shutdown op. uninstall deletes LOCK_PATH (cli.py:570), which the daemon writes once at startup (daemon.py:1734) and never rewrites, so the orphan (still holding hotkeys and the Global\Sonara-Daemon-Singleton-v1 mutex, daemon.py:1864/transport.py:104) becomes permanently unreachable. After reinstall, ensure_running() spawns a daemon whose main() sees the mutex held and exits instantly (daemon.py:1865-1866) — Sonara is silently dead until logoff/reboot or manual kill of BOTH orphans (supervisor_loop.py:91-102 restarts a killed daemon). The silent-failure claims also hold: the Task XML WorkingDirectory is APP_DIR\sonara\platform\windows (supervisor.py:80,103), the daemon inherits that cwd, so rmtree(APP_DIR) fails and is swallowed (cli.py:590-591); LOG_PATH is held open as daemon stderr (supervisor_loop.py:71) so its os.remove fails silently (cli.py:581-582); yet cli.py:600-601 prints "Removed Sonara runtime files" unconditionally. The project's own deploy runbook note (stop the live daemon before redeploying or copytree leaves platform/windows empty) corroborates the root cause in practice. Mitigating factors keeping it at important, not critical: the state self-heals at next logon, and uninstall without reinstall mostly works apart from the lingering process and false success output.

### [IMPORTANT] src/sonara/cli.py:637 — Ctrl+C during voices install is not reverted: `except Exception` (cli.py:623, 637) misses KeyboardInterrupt, and uv creates python.exe first, so a half-built venv passes the existence-based 'provisioned' checks and gets wired in.

Both provisioning branches catch only Exception, so a KeyboardInterrupt during the multi-GB downloads (chatterbox_provision.provision's 4-step sequence, kokoro provision/predownload) skips the uninstall_* revert. Because `uv venv` creates Scripts/python.exe before any packages install, neural_enabled() (kokoro_provision.py:21-23) and is_provisioned() (chatterbox.py:39-41) then return True: a later `sonara install` rewires the daemon onto the empty kokoro venv (cli.py:356-361 probes only the version, not the packages), and doctor reports chatterbox 'ready'. (Matches pending task #8 'revert on BaseException'.)

**Fix:** Catch BaseException (re-raising after revert for KeyboardInterrupt), or write a 'provisioned' marker file only after warmup/health succeeds and derive enabled-state from that marker.

**Verifier:** Confirmed. cli.py:623 and cli.py:637 catch only Exception, so KeyboardInterrupt during install_chatterbox()/install_kokoro() skips the uninstall_* revert (main() catches only OSError, no outer handler). Both provision() functions run `uv venv` first (chatterbox_provision.py:49, kokoro_provision.py:72), so Ctrl+C during the subsequent multi-GB pip installs/warmup leaves Scripts/python.exe on disk; neural_enabled() (kokoro_provision.py:21-23) and is_provisioned() (chatterbox.py:39-41) are pure existence checks and return True for the half-built venv. A later `sonara install` then wires the daemon onto the empty kokoro venv because _daemon_python (cli.py:350-361) probes only the interpreter version, and doctor reports chatterbox "ready" (cli.py:297-304) with no import/health probe. The only mitigations found are partial: the kokoro doctor branch does run neural_healthy and would flag the venv, and on Windows the uv child sometimes dies first yielding a caught CalledProcessError, but the parent KeyboardInterrupt path is the common outcome. Matches the repo's own acknowledged pending task #8. Severity "important" stands: silent persistent broken state plus a false-positive doctor check, but requires user Ctrl+C during install and is recoverable by uninstall/reinstall.

### [IMPORTANT] src/sonara/cli.py:302 — Doctor's chatterbox row claims 'ready' purely because the venv's python.exe exists - it never imports the stack or checks CUDA, so the documented silent-CPU-downgrade and half-install states all show green.

cli.py:296-308 only tests os.path.exists(paths.chatterbox_venv_python()). This is exactly the failure class chatterbox_provision.py:10-19 warns about: a venv where chatterbox-tts downgraded torch/torchaudio to CPU builds imports fine and only torch.cuda.is_available() reveals the breakage; an interrupted install also has python.exe present with nothing else. Contrast the kokoro row, which actually probes the venv via neural_healthy() subprocess (cli.py:283-291, kokoro_provision.py:95-102). So the one engine with a known silent-degradation hazard is the one doctor cannot detect.

**Fix:** Add a chatterbox_healthy() probe mirroring neural_healthy(): run venv python -c 'import torch, chatterbox; print(torch.cuda.is_available())' with a timeout, and report CPU-only/import failure as FAIL with the reinstall command.

**Verifier:** Confirmed, and worse than stated: cli.py:297 gates on cb.is_provisioned(), which is itself os.path.exists(chatterbox_venv_python()) (chatterbox.py:39-41), so the second os.path.exists(py) at cli.py:302 is a tautology and the FAIL branch (cli.py:305-308) is unreachable dead code — the chatterbox row can never report FAIL once the venv python exists. The CPU-downgrade hazard is documented in chatterbox_provision.py:10-19 (import succeeds; only torch.cuda.is_available() reveals it). Install-time protection (warmup() CUDA assert + revert at cli.py:622-626) does not refute it: the revert is `except Exception`, which misses KeyboardInterrupt (Ctrl+C mid multi-GB download leaves python.exe present, no revert), and post-install breakage (driver updates) is exactly doctor's job. Runtime Kokoro fallback with a notice means the breakage is not fully silent in use, but that makes doctor's false green more misleading, not less — a user diagnosing repeated fallbacks gets 'ready'. Kokoro's row genuinely probes via neural_healthy() subprocess (cli.py:283-291, kokoro_provision.py:95-102), so the proposed mirroring fix fits the existing pattern (warmup's probe snippet at chatterbox_provision.py:81-92 is nearly the needed code).

### [IMPORTANT] src/sonara/platform/windows/supervisor.py:815 — Doctor tells the user to 'run sonara start' but no start subcommand exists; the CLI has no daemon lifecycle commands at all, and the generic dead-daemon hint 'Run: sonara install' is wrong advice on an already-installed machine.

supervisor.py:815 emits "not running (run 'sonara start')" while _build_parser/_register_local (cli.py:175-222, 670-697) register no 'start' (or 'restart'/'stop-daemon'): following doctor's advice yields an argparse 'invalid choice' error, exit 2. The other hint used everywhere (cli.py:44-45, client.py:19-21, cli.py:254) says 'Run: sonara install', which on an installed machine triggers the full reinstall (and finding #1's APP_DIR corruption) when all that's needed is starting the daemon - client.ensure_daemon()/daemon.ensure_running() already exists (client.py:43-51) but is not exposed as a command. This missing lifecycle surface is also the root enabler of findings 1, 3 and 4 (no supported way to stop the daemon before mutating files it holds).

**Fix:** Add `sonara start` (call client.ensure_daemon) and `sonara stop-daemon`/`restart`; make doctor and DaemonNotRunning hints point at them.

**Verifier:** Confirmed. supervisor.py:815 tells the user to run 'sonara start', but the full CLI parser (cli.py _build_parser 175-222 + _register_local 670-697) registers no 'start' subcommand (only status/verbosity/rate/voice/minqueue/audio-control/duck-level/summary/repeat/stop/skip/doctor/install/uninstall/daemon/keymap/voices), so following doctor's advice yields an argparse invalid-choice error (exit 2). No alias or alternate handling exists: 'stop' stops speech not the daemon, 'daemon' is foreground-only, and client.ensure_daemon() (client.py:43-51) is never exposed as a command. The fallback hint 'Run: sonara install' (client.py:19-21, cli.py:44-45) is also bad advice, and worse than stated: per the comment at supervisor.py:847-850, schtasks /create does not auto-start the task, so install leaves the daemon down until next logon or a lazy-start hook. Both remediation hints for a dead daemon are broken; severity 'important' stands.

### [IMPORTANT] src/sonara/platform/windows/supervisor.py:874 — No stop/shutdown path exists anywhere: uninstall deletes the task definition but never ends the running supervisor or daemon, so the system keeps respawning after uninstall

WinSupervisorBackend.uninstall() (supervisor.py:874-886) calls task_uninstall() (schtasks /delete /f) but never '/end's the running task instance and never terminates the daemon. The supervisor loop is 'while True' with no exit condition and no awareness of install state (supervisor_loop.py:91-102), so after 'sonara uninstall' it keeps respawning the daemon until logoff. Worse, each respawn calls paths.ensure_sonara_dir() and reopens speechd.log (supervisor_loop.py:70-71), re-creating the very ~/.sonara artifacts cli.uninstall just deleted (cli.py:569-582), and the daemon restarts its global hotkeys. There is also no CLI command to stop the daemon at all (the 'stop' subcommand at cli.py:215 only stops speech). grep confirms zero taskkill/TerminateProcess/terminate calls in cli.py.

**Fix:** Add a stop primitive: schtasks /end /tn Sonara.Speechd before /delete, plus terminate the daemon (a 'shutdown' op over the socket, or TerminateProcess on the pid recorded in SINGLETON_PATH by transport.acquire_singleton). Call it from uninstall() and install(). Give the supervisor loop an exit condition (e.g. stop when the task definition or app dir is gone).

**Verifier:** Confirmed. uninstall() (supervisor.py:874) only runs schtasks /delete via task_uninstall() (supervisor.py:120-125); it never /end's the running task instance and never terminates the supervisor or daemon. The codebase itself acknowledges /delete-/create do not stop running instances: install() explicitly calls self._schtasks(["/end", ...]) at supervisor.py:851 before /create, and uninstall() lacks the equivalent. The supervisor loop (supervisor_loop.py:91-102) is `while True` with no exit condition, the task XML sets ExecutionTimeLimit PT0S (no limit), and each respawn re-runs paths.ensure_sonara_dir() + reopens speechd.log (supervisor_loop.py:69-71), recreating artifacts cli.uninstall() deleted (cli.py:569-582). No shutdown op exists in the protocol (protocol.py MsgType has none; STOP only clears speech, cli.py:215), Windows hotkey.uninstall() only stops hotkeys in the CLI process, and there are zero taskkill/TerminateProcess calls in the uninstall path; the plugin's commands/uninstall.md delegates to the same CLI. One detail in the finding is off: install() already has the /end half, so only uninstall() needs the stop primitive. Severity lowered from critical to important because the impact is bounded: hooks are removed at uninstall so no new speech arrives, and the deleted task never relaunches after the next logoff/reboot, so the lingering daemon/hotkeys/log recreation self-heal at session end with no data loss or security exposure.

### [IMPORTANT] src/sonara/platform/windows/supervisor_loop.py:91 — Stuck non-serving daemon is undetectable and unrecoverable: a mutex-holding daemon whose socket/lockfile is gone silences the system forever while the supervisor spawns doomed processes every 120s

daemon.main() exits immediately when it cannot take the named mutex (daemon.py:1813-1815, 'return' with exit code 0). Neither the supervisor loop (supervisor_loop.py:91-102, it only Popen+wait+sleep) nor the lazy-start path (ensure_running, daemon.py:1729-1734) ever checks WHY the child died or whether the mutex holder is actually serving. So a daemon that holds Global\Sonara-Daemon-Singleton-v1 but stopped serving (hung listener, or its daemon.lock was deleted -- cli.uninstall deletes LOCK_PATH at cli.py:570 while the daemon may still be running) makes socket_connectable() false forever: every hook event and every supervisor iteration spawns a fresh daemon that exits instantly on the mutex check. Result: permanent silence until a manual taskkill, plus a no-op pythonw spawn every <=120s indefinitely. This is exactly the known live pain ('repeated dev restarts left stuck non-serving daemons') and it is structural, not incidental.

**Fix:** Health-check the holder: the mutex loser (and the supervisor after N fast exits) should read the pid from SINGLETON_PATH, test socket_connectable(), and if the holder is alive but not serving for >grace period, TerminateProcess it and retry. Alternatively have the daemon watch its own lockfile/listener and self-exit when either dies, letting the supervisor respawn cleanly.

**Verifier:** Confirmed structural: daemon.main() exits code 0 when the named mutex is held (daemon.py:1864-1866); the supervisor loop (supervisor_loop.py:91-102) is Popen+wait+backoff with no exit-code or health inspection, and ensure_running (daemon.py:1767-1772, called from client.py:46 on every hook send) only tests socket_connectable before spawning. socket_connectable depends entirely on LOCK_PATH, which the daemon writes once at startup (daemon.py:1733) and never monitors, and cli.uninstall (cli.py:570) deletes LOCK_PATH without ever stopping the running daemon (WinSupervisorBackend.uninstall at supervisor.py:874-886 only removes the schtasks task/hooks/launcher). So a live mutex-holding daemon whose lockfile is gone makes every future spawn exit instantly, producing permanent silence plus a no-op pythonw spawn every <=120s, with no CLI stop/restart command to recover. One sub-claim is overstated: a dead accept loop self-heals (run() detects the dead thread, unlinks LOCK_PATH, exits, freeing the mutex), and a deadlocked process with a live listener keeps connectable() True (no spawn storm). Severity downgraded to important rather than critical: the state requires an out-of-band trigger (uninstall-while-running, dev redeploy, manual lockfile deletion) rather than normal operation, and logoff/reboot releases the mutex and heals it; but it fully disables the product with zero diagnostics (doctor says not running, reinstall appears to succeed) and no in-product recovery, which keeps it well above minor.

### [IMPORTANT] src/sonara/platform/windows/supervisor_loop.py:97 — Supervisor cannot distinguish 'another daemon is already serving' from a crash loop; it respawns a no-op process every <=120s forever whenever a lazy-started daemon owns the socket

If a hook lazy-started the daemon first (ensure_running, daemon.py:1729), the supervisor's own child exits instantly at daemon.main()'s socket_connectable() fast-path (daemon.py:1806-1807). run_supervisor_loop treats every <300s run as a failure (attempt += 1, supervisor_loop.py:97-102) and retries at the 120s cap indefinitely: a full pythonw process spawn + sonara import every 2 minutes for the life of the logon session, achieving nothing. It also means the backoff never converges to idle -- there is no 'someone else is healthy, stand down' state. This churn is also what turns the stuck-holder scenario above into a perpetual 720-spawns/day loop.

**Fix:** In run_supervisor_loop, before respawning check paths.socket_connectable(): if a daemon is serving, sleep and poll (no spawn) until it stops serving, then spawn. That single check removes the churn and makes fast-exit genuinely mean 'crash'.

**Verifier:** Confirmed. run_supervisor_loop (supervisor_loop.py:91-102) has no socket_connectable() check: it unconditionally spawns pythonw -m sonara.daemon, and daemon.main() fast-exits when another daemon owns the socket (daemon.py:1857-1858) or the singleton mutex (1864-1866). Every <300s exit increments attempt, so the loop respawns a no-op process at the 120s cap for as long as a non-supervised daemon serves. That state is realistically reachable: ensure_running (daemon.py:1767, called from client.py:46 on every hook event) spawns the daemon directly via WinSupervisorBackend.launch_spec (supervisor.py:737), so any hook firing during the supervisor's 2-120s backoff sleep after its own child dies installs an unsupervised socket owner. Nothing handles this elsewhere (Task Scheduler XML just launches the loop; supervisor.py:128-130 confirms the loop is the sole restarter). Minor wording nuance: the churn ends if the lazy daemon ever exits (the supervisor's next child then wins the mutex), so it is 'for the lazy daemon's lifetime' rather than strictly forever — but that lifetime is unbounded. Aggravating detail: each no-op child runs _arm_faulthandler() before the fast-path check (daemon.py:1849), truncating faulthandler.log (mode 'w', daemon.py:1792) every 120s and destroying any crash dump from the real daemon. Severity 'important' stands.

### [MINOR] src/sonara/platform/windows/supervisor_loop.py:71 — speechd.log grows without bound: both launch_spec variants append daemon stderr to it and nothing in the codebase rotates or caps it

supervisor_loop.launch_spec (line 71) and WinSupervisorBackend.launch_spec (supervisor.py:754) open paths.LOG_PATH in 'a' mode as the daemon's stderr. A repo-wide grep for rotate/truncate/MAX_LOG over LOG_PATH finds nothing; the only deletion is cli.uninstall (cli.py:571). The daemon writes speak-loop catch-all tracebacks there by design (#20), so a crash loop (the very thing the supervisor exists for) appends a traceback every restart, and a long-lived install accretes indefinitely in the user profile. The same applies to hotkeyd.log/faulthandler.log referenced at cli.py:574-575.

**Fix:** Rotate at spawn time -- the handle is reopened on every restart, which is the natural rotation point: if LOG_PATH exceeds ~2-5 MB, os.replace it to speechd.log.1 (overwriting the previous .1) before opening. ~6 lines in a shared helper used by both launch_spec copies.

**Verifier:** Confirmed: supervisor_loop.py:71 and supervisor.py:754 both open paths.LOG_PATH in append mode as daemon stderr, and no code in src/ ever rotates, truncates, or caps it — the only deletion is cli.py uninstall (line 571), which also covers hotkeyd.log/faulthandler.log. The log grows in normal operation too, not just on crashes: daemon.py:1210/1224 writes a [summary] line per finished turn, tts.py:495-531 writes per-utterance fallback lines, and five catch-all traceback sites plus earcon.py append on errors; a crash loop (backoff capped at 120s) appends a traceback up to ~720 times/day. Refutation attempts failed: the chatterbox worker's torch stderr spew is DEVNULL'd (chatterbox.py:279) but that only slows growth. However, severity is overstated: growth is ~200 bytes/turn normally (single-digit MB/year) and low single-digit MB/day in the worst crash-loop case, nothing reads the log at runtime, and the impact is slow disk accumulation with zero functional degradation — a real hygiene defect on an always-on daemon, but minor, not important. The proposed spawn-time size-check + os.replace rotation in a helper shared by both launch_spec copies is the right fix.

### [IMPORTANT] src/sonara/platform/windows/supervisor.py:851 — install() over a live system does not retire the old daemon: schtasks /end only touches the task-launched supervisor, so a lazy-started daemon survives on OLD code and blocks the new one via the mutex

The comment at supervisor.py:848-851 claims the best-effort '/end' stops a stale daemon on the old interpreter, but /end only ends the task instance (the supervisor pythonw). A daemon started by the lazy-start path (ensure_running from a hook or CLI, daemon.py:1729) is not a task child: it survives install, keeps the singleton mutex, and keeps executing the OLD code while cli._copy_app rmtree+copytree replaces ~/.sonara/app underneath it (cli.py:419-425) -- the exact failure captured in the deploy runbook ('stop it before redeploying or copytree leaves platform/windows empty'). Even for the task-launched case, the daemon child was spawned DETACHED (supervisor_loop.py:24) and whether /end kills it depends on Task Scheduler's job-object behavior; the code should not rely on it.

**Fix:** Reuse the stop primitive from the uninstall finding: before _copy_app, explicitly end the supervisor task AND terminate/ask-to-exit the daemon (socket shutdown op or pid from SINGLETON_PATH), then verify socket_connectable() goes false before copying.

**Verifier:** Confirmed. install() never retires a live daemon: _copy_app (cli.py:502, rmtree+copytree at 419-425) runs at step 2, before sup.install() at step 5, so the schtasks /end at supervisor.py:851 fires AFTER the destructive copy — and /end only targets the task instance anyway. The lazy-start path is real and hot: client.py:46 calls ensure_running() on every hook send, which spawns the daemon directly (daemon.py:1767-1772 via launch_spec, supervisor.py:737-762), not as a task child, so /end cannot reach it. That surviving daemon holds the named mutex Global\Sonara-Daemon-Singleton-v1 (daemon.py:1864); a new daemon exits silently at daemon.py:1865-1866, and the socket_connectable() fast paths (daemon.py:1768, 1857) prevent any new spawn attempt — old code keeps running until logoff/reboot. There is no daemon-exit primitive anywhere: 'sonara stop' (cli.py:128) only sends a speech STOP; protocol.py has no shutdown op. The failure is observed live per the deploy runbook (copytree under a live daemon left platform/windows empty). Only soft spot: /end plausibly DOES kill the task-launched child (Task Scheduler job tree; DETACHED_PROCESS does not break out of a job object), but the finding hedges that and the lazy-start hole plus copy-before-/end ordering stand independently. Severity 'important' is appropriate: hits every plugin update over a live system, workaround is manual kill/reboot.

### [IMPORTANT] src/sonara/platform/windows/supervisor.py:747 — launch_spec and _hook_py derive paths from paths.repo_root(), which is wrong when sonara is imported from the installed APP_DIR layout (~/.sonara/app/sonara has no src/)

paths.repo_root() hardcodes the <repo>/src/sonara layout (paths.py:66-73), but _copy_app installs the package as app_dir/sonara with no src/ (cli.py:420-425), and sonara.cmd runs the CLI with PYTHONPATH=app_dir (supervisor.py:862-872). From that layout repo_root() returns ~/.sonara, so: (a) WinSupervisorBackend.launch_spec sets the spawned daemon's PYTHONPATH to the nonexistent ~/.sonara/src (line 747-749) -- the daemon only imports successfully because sonara.cmd's own PYTHONPATH=app_dir happens to be inherited and appended; strip that inheritance and every CLI-triggered lazy start becomes the documented 'relaunch storm'; (b) 'sonara install' re-run from the launcher fails at _copy_app because ~/.sonara/src/sonara does not exist (cli.py:497-506); (c) _hook_py() (supervisor.py:665-668) would bake a nonexistent ~/.sonara/bin/sonara-hook into settings.json if reached from that layout. supervisor_loop._package_root (supervisor_loop.py:27-33) already solves this correctly by deriving from __file__ for both layouts.

**Fix:** Derive the import root from the loaded package, not the repo layout: root = os.path.dirname(os.path.dirname(sonara.__file__)) works for both src/ and app_dir layouts (it is exactly _package_root's logic). Use it in WinSupervisorBackend.launch_spec, and make _hook_py/_copy_app fail loudly (or resolve via the install record) when run from the app copy.

**Verifier:** Confirmed. paths.repo_root() (paths.py:66-73) hardcodes the <root>/src/sonara layout, but the launcher (supervisor.py:862-872) runs the CLI with PYTHONPATH=~/.sonara/app where the package lives at app/sonara with no src/, so repo_root() returns ~/.sonara. Verified consequences: (b) is real and worse than stated — re-running 'sonara install' via the launcher (which the CLI's own messages instruct, cli.py:45/254/328) computes plugin_root=~/.sonara, and _copy_app (cli.py:423-425) rmtree's ~/.sonara/app/sonara BEFORE copytree fails on the nonexistent ~/.sonara/src/sonara, bricking the install (Task Scheduler points at the deleted supervisor_loop.py) with a misleading error. (a) launch_spec (supervisor.py:747-749) does prepend the nonexistent ~/.sonara/src and works only via the inherited app-dir PYTHONPATH — latent fragility, not live breakage (hook-triggered starts are safe since bin/sonara-hook pins the plugin src/ on sys.path). (c) is largely defused: install fails before merge_hooks_into_settings can write a bad hook path, and uninstall's remove_hooks_from_settings matches by the SONARA_HOOK_MARKER sentinel, not the path. supervisor_loop._package_root (supervisor_loop.py:27-33) proves the layout-agnostic derivation already exists; the two 'parity' launch_spec paths genuinely diverge. Severity 'important' stands on sub-claim (b) alone.

### [IMPORTANT] src/sonara/platform/windows/supervisor.py:737 — Two hand-synced copies of launch_spec (supervisor.py:737-762 and supervisor_loop.py:51-80) that have already diverged in how they derive the PYTHONPATH root

The bodies are near-identical (same spawn flags, same stderr-to-log rationale, same env dance) but supervisor.py uses repo_root()+'/src' while supervisor_loop uses the __file__-derived _package_root(). That divergence is not cosmetic -- it is the root cause of the layout bug above, and any future change (log rotation, spawn flags) must be made twice. supervisor_loop.py cannot import sonara at module load (it is launched by bare script path), but _ensure_importable() already fixes sys.path before _main runs, so it CAN import a shared helper at call time.

**Fix:** Keep one implementation in supervisor_loop.py (the file that must stand alone), parameterized by pythonw + root, and have WinSupervisorBackend.launch_spec import and delegate to it, passing _package_root-style derivation.

**Verifier:** Confirmed. Two hand-synced copies of launch_spec exist (supervisor.py:737-762 and supervisor_loop.py:51-80) with duplicated _SPAWN_FLAGS constants, identical env/stderr/spawn logic, and a comment in supervisor_loop.py:61 admitting the by-hand parity. The root-derivation divergence is behaviorally real: the installed layout is ~/.sonara/app/sonara (cli.py:_copy_app, no src/ level), and the sonara.cmd launcher (supervisor.py:867) runs the CLI with PYTHONPATH=~/.sonara/app; in that process paths.repo_root() resolves to ~/.sonara, so supervisor.py's launch_spec sets PYTHONPATH to the nonexistent ~/.sonara/src, and a lazy-started daemon (client.py:46 -> daemon.py:1767 ensure_running) exits instantly on import failure - the relaunch-storm failure the function's own comment warns about. supervisor_loop's _package_root() handles both dev and installed layouts correctly. The hook path only works by accident (bin/sonara-hook inserts the plugin cache's src/), and test_win_supervisor.py:81 locks in the dev-layout assumption so tests miss the installed-layout breakage. The proposed consolidation is feasible: supervisor_loop has no top-level sonara import and already defers 'from sonara import paths' to call time, so WinSupervisorBackend.launch_spec can delegate to it. Severity 'important' is appropriate.

## Unverified non-minor (over verification cap)

- **[important] src/sonara/platform/windows/supervisor.py:1** — supervisor.py is four unrelated modules in one 926-line file; the settings.json hook-merge machinery (~380 lines) is the obvious extraction
  - Natural seams, each already delimited by banner comments: (1) Task Scheduler XML autostart, lines 36-130; (2) Windows Python resolution (store-stub detection, py-launcher, version probing), lines 133-253; (3) hooks.json template + settings.json read/validate/merge/remove machinery, lines 256-648 -- a self-contained subsystem with its own sentinel protocol (SONARA_HOOK_MARKER), atomic-write helper, and validation, none of which touches the supervisor; (4) launcher + WinSupervisorBackend, lines 651-926. Section 3 alone is ~390 lines and is pure JSON-file surgery used by install/uninstall/doctor; extracting it (e.g. hooks_settings.py) plus section 2 (pyresolve.py) drops supervisor.py under ~350 lines and gives the hook-merge logic a home its tests can target directly.
  - Fix: Extract sections 2 and 3 into sibling modules (pyresolve.py, hooks_settings.py) with supervisor.py re-exporting for compatibility.
- **[critical] src/sonara/platform/windows/earcons/generate.py:108** — Daemon fires earcon kinds 'nav', 'nav_edge', 'session_change', and 'summary_failed' that have no bundled wav, so those beeps are silent no-ops
  - _EARCON_SPECS defines only 6 earcons (permission, choice, plan, error, turn_done, ready), and the earcons directory contains exactly those 6 wavs. default_earcons() (earcons/__init__.py:35-69) therefore returns only those keys, and neither config.py DEFAULTS nor cli.py ever adds an 'earcons' key, so daemon.py:1829-1830 installs exactly that 6-entry map. But the daemon fires speaker.earcon('nav') / 'nav_edge' at daemon.py:592, 598, 601, 727, 745, 'session_change' at daemon.py:1556, and 'summary_failed' at daemon.py:1206. speaker.earcon() (speaker.py:102-104) does self._earcons.get(kind) and silently returns when the kind is absent. So every nav press, nav boundary, session switch, and summary failure gives NO audible feedback, despite the code's explicit intent ('Every nav press chimes', daemon.py:594-595) - for an eyes-free user this is invisible breakage with no log.
  - Fix: Add the four missing kinds to _EARCON_SPECS and regenerate/commit the wavs (or alias them to existing wavs in default_earcons()). Additionally, have speaker.earcon() log unknown kinds to stderr so a spec/usage divergence is diagnosable.
- **[important] src/sonara/platform/windows/hotkeys.py:145** — stop() can run before the pump thread sets _thread_id, so WM_QUIT is never posted, the join times out, and a reload re-triggers the exact H2 'all hotkeys dark' failure the code guards against
  - stop() (hotkeys.py:139-150) calls _post_quit(), which silently does nothing when self._thread_id is None (hotkeys.py:73-76). _thread_id is only set inside the pump thread (hotkeys.py:120) after the thread is scheduled. If a RELOAD_KEYMAP arrives shortly after start() (daemon startup + immediate `sonara keymap` change), stop() posts nothing, GetMessageW blocks forever, join(timeout=2.0) expires, and stop() sets self._thread = None while the old thread still owns every registered chord. The subsequent start() then gets ERROR_HOTKEY_ALREADY_REGISTERED (1409) on ALL chords - the exact H2 failure documented at hotkeys.py:139-144: hotkeys dark until daemon restart, plus doctor reports every action as 'chord already owned by another app'. There is also a second, related window: PostThreadMessageW fails if the target thread has not yet created a message queue (created lazily by the first message-API call), so even a set _thread_id does not guarantee delivery before the first GetMessageW.
  - Fix: Have _run() set a threading.Event after capturing the thread id and forcing message-queue creation (a PeekMessageW call before registering), and make stop() wait on that event before _post_quit(). Do not null self._thread when join() timed out - keep the handle so a later stop can retry.
- **[important] src/sonara/keymap.py:97** — One bad entry in keymap.json silently disables ALL hotkeys: resolve_keymap raises on the first unknown key/modifier, start() aborts, and daemon._start_hotkeys swallows the exception without a log
  - resolve_keymap raises ValueError on any unknown key (keymap.py:97) or modifier (keymap.py:102). hotkeys.start() resolves the whole map up front (hotkeys.py:115), so the exception aborts the entire hotkey subsystem, and daemon._start_hotkeys catches it with a bare pass (daemon.py:954-955) - no log line, no earcon. The user hand-edits keymap.json (the doctor message at hotkeys.py:202 explicitly tells them to), typos 'ctl', writes "mods": "ctrl" (list('ctrl') becomes ['c','t','r','l'] via keymap.py:135, then 'unknown modifier: c'), or picks any key outside the table - and every hotkey including mute goes dark. Worse, because start() raised before _write_state() ran, doctor then reports 'daemon not running; start it...' (hotkeys.py:189-191) while the daemon IS running, pointing the user away from the real cause.
  - Fix: Make resolve_keymap skip invalid entries and return (resolved, errors); register the valid chords and surface the per-action errors through the hotkeys.state.json diagnostics that doctor already reads. At minimum, log the traceback in daemon._start_hotkeys instead of a bare pass.
- **[important] src/sonara/keymap.py:99** — A binding with no modifiers registers a bare global key (e.g. plain 'M') that swallows that keystroke system-wide in every application
  - resolve_keymap builds mask=0 and accepts an empty mods list (keymap.py:98-103); nothing requires at least one modifier. load_keymap makes this easy to hit by accident: a user override {"mute": {"key": "m"}} that merely omits 'mods' fully replaces the default binding with mods=[] (keymap.py:133-136) rather than inheriting the default chord. The resolved entry then reaches RegisterHotKey with fsModifiers = MOD_NOREPEAT only (hotkeys.py:93-94), which registers unmodified 'M' globally - the letter m stops working in every app on the machine, with no warning and no obvious connection to Sonara.
  - Fix: In resolve_keymap, reject (or route through the finding-3 per-entry error channel) any bound entry whose modifier mask is 0. If bare-key chords should stay possible, gate them behind an explicit "allow_no_mods": true flag.
- **[important] src/sonara/platform/transport.py:21** — Lockfile 0o600 chmod is a no-op on Windows; token file is world-readable per POSIX bits and the perms test fails (expects 600, gets 666)
  - write_lockfile does os.chmod(tmp, 0o600), but on Windows chmod only maps to the FILE_ATTRIBUTE_READONLY bit; group/other bits are ignored, so lock.stat().st_mode shows 666 and tests/test_transport.py:11 (assert oct(...)[-3:] == '600') fails on the platform this ships on. The module docstring (transport.py:3) advertises 'mode 0o600' as part of the security story, but the 256-bit token's actual confidentiality rests entirely on the inherited ACL of %USERPROFILE%\.sonara. That inherited ACL is usually user-only, but nothing verifies it, and the false claim hides the gap.
  - Fix: Set an explicit DACL on the lockfile (icacls via subprocess, or ctypes SetNamedSecurityInfo restricting to the owner SID), or verify/repair the ~/.sonara directory ACL at daemon start; update the docstring and rewrite the test to assert the Windows-real property (ACL) instead of POSIX mode bits.
- **[important] src/sonara/platform/transport.py:41** — Clients never authenticate the server: a stale lockfile pointing at a port now held by another process receives the token and all session content, and ensure_daemon thinks the daemon is up
  - connect() (transport.py:38-42) reads host/port from the lockfile and immediately sends the token; connectable() only checks that connect+token-send succeeded, with no reply expected. If the daemon dies (or after a reboot where autostart fails) the lockfile persists and its ephemeral port can be re-bound by any other local process. Every subsequent hook event then (a) hands that listener the token, (b) streams session content to it - hooks_entry messages carry prose deltas, Bash command text, and file paths (hooks_entry.py:36-70), and (c) client.ensure_daemon (client.py:43-51) sees _connectable() True and never calls ensure_running(), so the real daemon is never respawned: Sonara goes silently and permanently mute while leaking session text.
  - Fix: After the token line, require a one-line server hello (e.g. daemon echoes a hash of the token) before connectable() returns True or send() writes a message; additionally cross-check the lockfile pid is alive before trusting it, and have the supervisor delete the lockfile on clean daemon exit.
- **[important] src/sonara/platform/windows/ducking.py:95** — duck(): an exception mid-loop leaves already-lowered sessions ducked forever - they are recorded nowhere, so neither restore() nor the crash sweep can undo them
  - The whole enumeration/lower loop (ducking.py:95-107) sits inside one try. If GetMasterVolume/SetMasterVolume raises for session N (session ended between enumeration and the call, COM error), sessions 1..N-1 have already been lowered but the except at line 108 discards the local `saved` list: self._saved stays [], _write_state() is never called (no crash-sweep record on disk), and self._ducked stays False. Those apps stay at the duck volume until the user manually fixes each one. Worse, _ducked==False means the next _maybe_duck() ducks again and may record the already-lowered volume as the 'original', baking the ducked level in permanently.
  - Fix: Move the try/except inside the per-session loop body (skip a bad session, keep the rest), and append to self._saved / write the state record incrementally before lowering each session so every lowered session is always restorable.
- **[important] src/sonara/platform/windows/ducking.py:48** — COM thread-affinity bug: after a startup crash-sweep, duck() on the speak-loop thread calls CoCreateInstance on a COM-uninitialized thread and ducking silently no-ops for the daemon's lifetime
  - comtypes initializes COM only on the thread that first imports it. restore_from_state_file() runs on the daemon's main thread at startup (daemon.py:1839-1840) and, when a duck_state.json exists, imports comtypes there. duck() is then called from the speak-loop thread (daemon.py:1506-1508, thread started at daemon.py:1687) and from TCP-handler threads (set_duck_level re-duck at daemon.py:865-866); on those threads CoCreateInstance raises CO_E_NOTINITIALIZED, which the blanket except at ducking.py:108 swallows, so audio control silently stops working exactly after the crash-recovery path ran. Sharing the saved COM session pointers between the speak-loop thread (duck) and handler threads (restore at daemon.py:850) also violates apartment rules with errors likewise swallowed.
  - Fix: Call comtypes.CoInitialize() (paired with CoUninitialize, or CoInitializeEx with MTA) at the top of _all_sessions()/restore paths so every calling thread is initialized, and re-enumerate sessions in restore() instead of reusing cross-thread interface pointers (the state-file record already has pid/name/original).
- **[important] src/sonara/client.py:8** — Every hook process imports the entire daemon module tree and makes 2-3 TCP connections per event; MessageDisplay fires per prose delta, so this is the hot path
  - client.py:8 does top-level `from sonara.daemon import ensure_running`, so every bin/sonara-hook invocation (a fresh Python process per hook event, including every MessageDisplay delta) imports daemon.py (~1850 lines) plus sonara.queue, assembler, config, history and the platform package - all just to send one JSON line to an already-running daemon. On top of that, ensure_daemon() (client.py:43-46) performs a full connect+token+close probe via socket_connectable() before send() opens a second connection, and multi-message events (UserPromptSubmit, SessionStart, AskUserQuestion in hooks_entry.py:49-111) open a fresh connection per message: 3+ TCP handshakes for one event.
  - Fix: Move the sonara.daemon import inside ensure_daemon's failure branch (only needed when the daemon is down); replace the probe with try-send-first (on DaemonNotRunning: ensure_running() then retry); and let the hook shim send all of an event's messages over one connection (send() already accepts one socket - add a send_many or reuse the socket).
- **[important] src/sonara/daemon.py:1579** — Pre-auth token handshake buffers unbounded data from unauthenticated peers
  - _handle_conn's handshake loop (daemon.py:1579-1586) accumulates into buf until a newline arrives with no size cap. An unauthenticated localhost peer that sends newline-free data grows the daemon's memory without limit; the settimeout(5.0) is per-recv, so a steady drip keeps the connection alive indefinitely and a fast blast can push hundreds of MB before any timeout. The post-auth message loop (1590-1598) has the same unbounded buffering, though that peer at least holds the token. Also at daemon.py:1588 the token is compared with `!=` rather than a constant-time compare.
  - Fix: Cap the pre-auth line (e.g. 4 KB - the token line is 65 bytes) and drop the connection when exceeded; apply a sane max line length post-auth too; compare tokens with hmac.compare_digest.

## Minor findings (unverified)

- **src/sonara/cli.py:84** [false-confirmation] — Fire-and-forget setters print confirmations for values the daemon silently clamps or rejects: `sonara duck-level 250` prints 'set to 250 percent' while the daemon clamps to 100 (and speaks the different number); minqueue/rate behave the same.
  - _cmd_duck_level (cli.py:82-85), _cmd_minqueue (cli.py:69-72) and _cmd_rate (cli.py:64-66) send with expect_reply=False and validate nothing; the daemon clamps duck level to 0-100 (daemon.py:857-861), minqueue to MINQUEUE_MIN..MAX (daemon.py:832-841) and rate to RATE_MIN..MAX (daemon.py:803-809), or silently drops unparsable values. For an eyes-free tool the printed/spoken confirmations can disagree with each other and with reality. Similarly _cmd_voice's set path (cli.py:119) gives no feedback at all, valid or not.
  - Fix: Validate ranges in argparse (type functions raising ArgumentTypeError), or send with expect_reply=True and echo the value the daemon actually applied.
- **src/sonara/cli.py:350** [duplication] — Interpreter-selection policy is duplicated: cli._daemon_python (cli.py:350-361) and supervisor.daemon_pythonw (supervisor.py:194-204) implement the same neural-venv-else-system rule and can drift; cli also reaches into the private sup._probe_python_version (cli.py:359, 488).
  - Two copies of 'use the kokoro venv if provisioned and >=3.10' exist, differing only in python.exe vs pythonw.exe; a change to the policy (e.g. a chatterbox-aware rule) must be made twice. Related sync burdens acknowledged in comments: _WINRT_PACKAGES (cli.py:429-436) is manually mirrored in pyproject's [windows] extra and the tts.py hint string, and _daemon_not_running_message (cli.py:44-45) duplicates the DaemonNotRunning message in client.py:19-21. supervisor.py additionally has _probe_python_version both as a module function (line 168) and a method (line 686).
  - Fix: Move the policy into one function (e.g. paths or a small runtime module) parameterized on console/windowless, and expose probe_python_version publicly.
- **src/sonara/cli.py:225** [structure] — cli.py is two files in one: thin protocol-message commands (lines 39-172) plus ~440 lines of installer/doctor/provisioning orchestration (225-661); the installer half wants its own module.
  - The control commands are uniform 3-line handlers, while doctor()/install()/uninstall()/_cmd_voices_* carry subprocess management, file copying, install-record IO and pip bootstrap logic. Extracting an installer.py (or setup.py-style module) would keep cli.py as pure argument dispatch, shrink the import surface the fast control path pays for, and give the install/doctor logic room for the fixes above (daemon stop, health probes) without pushing the file toward supervisor.py's 926 lines. Also minor: `sonara voices install kokoro` can exit 0 without ever confirming health - cli.py:642-644 prints 'Neural voices ready' only when neural_healthy() passes but still returns rc 0 when it fails, so a broken venv yields a silent success exit.
- **src/sonara/platform/windows/supervisor.py:99** [edge-case] — Task XML is built by str.format with no XML escaping; a path or username containing '&' or '<' produces invalid XML and autostart registration silently degrades to a warning
  - task_install (supervisor.py:96-117) interpolates user_id, pythonw, supervisor_py, and work_dir straight into TASK_XML_TEMPLATE. '&' is legal in Windows directory and account names; the resulting XML fails schtasks /create, and install() only prints 'warning: schtasks /create returned N' (supervisor.py:855-857) so the user ends up with no autostart and a mostly-green install.
  - Fix: Escape the four values with xml.sax.saxutils.escape() before formatting.
- **src/sonara/platform/windows/supervisor.py:92** [error-path] — _current_user_id ignores the GetUserNameExW return value; on failure the task XML gets an empty Author/LogonTrigger UserId and registration fails confusingly
  - supervisor.py:87-93 calls ctypes.windll.secur32.GetUserNameExW(2, buf, byref(size)) without checking the BOOL result or sizing errors (a long domain\user can exceed 256 with ERROR_MORE_DATA); buf.value is then '' and schtasks rejects the XML with only the generic exit-code warning.
  - Fix: Check the return value; on failure fall back to os.environ['USERDOMAIN'] + '\\' + os.environ['USERNAME'], or retry with the size the API reports.
- **src/sonara/platform/windows/supervisor.py:147** [startup-cost] — resolve_python_windows can spawn 5-6 python subprocesses with 5s timeouts each on a single call; it sits on the lazy-start path so a daemon cold start can stall several seconds in the worst case
  - resolve_python_windows (supervisor.py:207-253) probes py-launcher, 'python', 'python3', each via subprocess with timeout=5, plus _is_store_stub's slow path runs the candidate again (supervisor.py:146-153) and _probe_python_version runs it a third time (line 168-179). daemon_pythonw() (line 194-204) calls it as the fallback whenever neural is disabled or the venv probe fails, and daemon_pythonw runs inside WinSupervisorBackend.launch_spec (line 740) -- i.e. inside the hook process on every lazy start. Normal case is ~2-3 fast spawns, but a broken PATH entry (hung stub) costs up to 5s per probe before speech starts.
  - Fix: Cache the resolved pythonw in the install record (it is already recorded at install time -- paths.recorded_pythonw is used as the last fallback at line 253) and consult the record first, probing only if the recorded path is missing.
- **src/sonara/platform/windows/supervisor_loop.py:101** [correctness] — Backoff math is off-by-one versus its docstring: after a healthy run (attempt=0) the next delay is 1s, not the documented base of 2s
  - run_supervisor_loop resets attempt=0 after a healthy run, then computes delay = min(BASE * 2**(attempt-1), CAP) = min(2 * 2**-1, 120) = 1.0s (supervisor_loop.py:97-101). Harmless in practice (a healthy daemon that exits should restart quickly), but the float exponent path is surprising and the '2, 4, 8...' docstring sequence only holds from the second failure onward.
  - Fix: delay = min(BASE * (2 ** max(attempt - 1, 0)), CAP), or restructure to compute the delay only on the failure branch.
- **src/sonara/platform/windows/keytables.py:5** [edge-case] — KEY_CODES covers only 9 letters, 3 punctuation keys, and arrows - most reasonable rebind targets (other letters, digits, F-keys) are 'unknown key' errors
  - KEY_CODES (keytables.py:5-16) omits 17 letters, all digits, and all function keys. Combined with finding 3's all-or-nothing resolution, a user who rebinds mute to 'n' or 'F9' in keymap.json does not get a helpful failure - every hotkey silently dies. Letters and digits are pure ASCII math and F-keys are a contiguous VK range, so the table's small size buys nothing.
  - Fix: Generate the full sets programmatically: {chr(c).lower(): c for c in range(0x41, 0x5B)} for letters, 0x30-0x39 for digits, f1-f24 at 0x70+, keeping the OEM/arrow aliases as explicit entries.
- **src/sonara/platform/windows/earcons/__init__.py:52** [error-path] — default_earcons() leaves a partially-populated module cache when a wav is missing: first call raises, second call silently returns an incomplete mapping
  - The loop writes into the module-global _cache per name (earcons/__init__.py:67) and raises FileNotFoundError mid-loop if a later wav is absent (61-66). Because the guard is `if _cache:` (52-53), any subsequent call returns the partial dict as if resolution succeeded, so the fail-loud contract only holds once per process. Separately, the first-call raise propagates through daemon main() unguarded (daemon.py:1829-1830), so one missing packaged wav turns into a daemon that exits at startup and gets respawned by the supervisor in a crash loop - no speech at all, which is a harsher outcome than degraded earcons.
  - Fix: Build a local dict and assign it to _cache only after all names resolve. Consider catching the error at daemon startup and continuing with whatever earcons resolved (plus a stderr log), since speech should outrank beeps.
- **src/sonara/platform/windows/hotkeys.py:159** [stale-state] — hotkeys.state.json survives a hard-killed daemon, so doctor reports 'no collisions (daemon-reported)' or stale collisions for a daemon that is not running
  - _clear_state only runs in the pump thread's finally (hotkeys.py:134), i.e. on clean shutdown. If the daemon is killed (supervisor kill, crash, power loss), the state file persists and doctor_rows() (hotkeys.py:183-205) treats it as live daemon-reported truth - asserting a green 'no collisions' or listing collisions from a previous boot. The docstring's claim 'gone -> doctor reports daemon not running' (hotkeys.py:134) only holds for graceful exits. The file also lacks any pid/timestamp for staleness detection.
  - Fix: Write the daemon pid (and a timestamp) into the state file and have doctor_rows() verify that pid is alive (and is a sonara daemon) before trusting the contents; fall back to the 'daemon not running' row otherwise.
- **src/sonara/platform/windows/earcon.py:36** [error-path] — play() returns None silently for a missing wav file while logging spawn failures - the exact 'silent missing earcon' the code's own comment calls untraceable
  - The missing-file early return (earcon.py:36-37) produces no diagnostic, while the spawn-failure path (50-56) prints a traceback precisely because 'for an eyes-free user a SILENT missing earcon is also untraceable'. The two failure modes have identical user-facing symptoms (no beep) but only one is diagnosable from the log. With finding 1 (kinds mapped to nonexistent paths would hit this branch if someone hand-configured them), this is the branch most likely to hide real misconfiguration.
  - Fix: Print a one-line 'earcon wav missing: <path>' to stderr before returning None.
- **src/sonara/platform/windows/earcon.py:41** [latency] — Every earcon spawns a fresh Python interpreter, adding ~50-150ms interpreter startup before the beep - noticeable lag for what the daemon treats as 'instant' feedback
  - play() launches `sys.executable -c "import winsound..."` per beep (earcon.py:41-49). The daemon's EARCON branch is documented as 'Instant' (daemon.py:479-488) and hotkey feedback beeps already traverse pump -> queue -> lock before reaching play(); a cold CPython start (plus process creation under load) delays the audible confirmation of every keypress and permission chime. Process-per-beep is the right isolation for mixing with winsound speech, but it does not need to be a new interpreter each time.
  - Fix: Keep one persistent windowless helper process (spawned lazily, fed wav paths over stdin, respawned if it dies) or pre-spawn the helper at daemon start; keep Popen-per-beep only as the fallback.
- **src/sonara/platform/windows/hotkeys.py:21** [duplication] — _VK_LABELS is a hand-maintained inverse of keytables.KEY_CODES; any key added to keytables displays as 'keyNN' in the CLI/doctor until this second table is updated
  - _VK_LABELS (hotkeys.py:21-26) duplicates the VK-code knowledge already in keytables.KEY_CODES (keytables.py:5-16) in inverted form, used only by display_combo (hotkeys.py:207-210). The two tables already require lock-step edits, and finding 5's table expansion would make manual maintenance worse.
  - Fix: Derive the label map by inverting keytables.KEY_CODES (first/canonical name per code, title-cased), keeping only genuinely display-specific overrides ('Space', 'Enter') local.
- **src/sonara/keymap.py:225** [dead-code] — write_resolved()/HOTKEYD_RESOLVED_PATH writes a 'Swift-facing' resolved keymap file that nothing in the repo ever reads - there is no macOS backend in the tree
  - keymap.write_resolved (keymap.py:225-238) is called from cli.py:512 during install and the file is deleted at uninstall (cli.py:572), but src/sonara/platform contains only the windows backend; the Windows path resolves the keymap in-process at hotkeys.start() (hotkeys.py:115) and never reads hotkeyd.resolved.json. Related vestige: resolve_keymap json.dumps each ACTION_MESSAGES entry (keymap.py:108) only for hotkeys._register_all to json.loads it straight back (hotkeys.py:95) - a leftover of the file/IPC protocol.
  - Fix: Drop write_resolved, HOTKEYD_RESOLVED_PATH (paths.py:13), and the cli call, or clearly fence them behind a future-macOS marker; pass ACTION_MESSAGES dicts through resolve_keymap without the dumps/loads round-trip.
- **src/sonara/platform/windows/hotkeys.py:69** [error-path] — A GetMessageW error (-1) is indistinguishable from WM_QUIT: the pump exits, unregisters all chords, and hotkeys go permanently dark with no log
  - _get_message returns None for both r==0 (WM_QUIT) and r==-1 (error) (hotkeys.py:69-70), and the pump loop breaks on None (hotkeys.py:126-127) then unregisters every chord in the finally. A transient user32 error therefore kills all hotkeys until a daemon restart, with zero diagnostic output; the only visible effect is _clear_state making doctor say 'daemon not running' while the daemon still runs.
  - Fix: On r==-1, log GetLastError to stderr (the daemon routes stderr to its log) and distinguish it from a clean quit; optionally retry a bounded number of times before giving up.
- **src/sonara/keymap.py:212** [maintainability] — write_default_keymap_if_absent materializes the FULL default keymap into the user's keymap.json, pinning defaults at install time and forcing a bespoke migration for every future default change
  - Because install writes all defaults into keymap.json (keymap.py:212-222), the file cannot distinguish 'user chose this' from 'installer wrote this', so any later default change needs a heuristic migration - migrate_default_chord (keymap.py:182-209) already exists solely to undo this for the Ctrl+Shift+Alt -> Ctrl+Alt change, and it guesses customization by exact-match. load_keymap already merges user entries over defaults (keymap.py:113-137), so an empty or absent keymap.json works fine; materializing it is pure downside. Small related nit: migrate_default_chord compares entry.get('key') == default_key case-sensitively (keymap.py:203) while resolve_keymap lowercases, so a hand-edited 'Left' escapes migration.
  - Fix: Stop materializing defaults at install (write '{}' or nothing; keep the file for user edits), and let migrate_default_chord remain for existing installs only. Lowercase the key comparison in the migration.
- **src/sonara/platform/transport.py:40** [robustness] — connect() raises KeyError/TypeError (not OSError) on a schema-invalid lockfile, escaping every caller's `except OSError`
  - read_lockfile only guards OSError/ValueError, so a lockfile that is valid JSON but missing 'host'/'port'/'token' (version skew, hand edit, or a non-dict) makes connect() raise KeyError or TypeError at transport.py:40-41. connectable() (transport.py:45-49) and client.send's DaemonNotRunning wrapper (client.py:17-21) both catch only OSError, so instead of the friendly 'daemon is not running' path the CLI surfaces a raw traceback (hook processes survive only because sonara-hook wraps main() in except Exception).
  - Fix: Validate the parsed dict in read_lockfile (require str host, int port, str token) and return None on any mismatch.
- **src/sonara/platform/windows/ducking.py:175** [correctness] — Crash-sweep restore by process name can overwrite the volume of unrelated new processes and applies one saved value to all same-name sessions
  - restore_from_state_file falls back to matching sessions by process name (ducking.py:169-176). Multi-process apps (chrome.exe) get every session forced to the single recorded value, and a brand-new process that merely shares an image name with a previously-ducked one has its user-set volume clobbered at daemon startup.
  - Fix: Restore by pid when the pid is still alive; use the name fallback only when the recorded pid no longer exists, and consider restoring name matches only up to 1.0-clamped sanity.
- **src/sonara/client.py:27** [robustness] — Reply read in send() is unbounded and decode errors propagate raw
  - The expect_reply loop (client.py:26-35) accumulates until a newline with no size cap, and decode() (protocol.py:49-51) lets json.JSONDecodeError escape, so a corrupt reply gives callers a ValueError rather than a clean failure. Low practical risk while the peer is the trusted daemon, but it compounds the missing server authentication (see transport.py:41 finding).
  - Fix: Cap the reply buffer (e.g. 1 MB), and wrap decode in try/except returning None on malformed replies.
- **src/sonara/platform/transport.py:62** [maintainability] — Stale/contradictory docs around the transport's security and single-instance story
  - Three misleading statements maintainers will trip on: (1) transport.py:3 claims the lockfile is 'mode 0o600' - false on Windows (see the chmod finding); (2) transport.py:62-67 acquire_singleton's docstring still says 'if msvcrt.locking proves unreliable, switch to a named mutex' as a future fallback, while acquire_singleton_mutex directly below (transport.py:84-110) is already the authoritative guard and the byte-lock survives only as a best-effort pid record (daemon.py:1816); (3) protocol.py:1 says the protocol runs 'over a Unix stream socket' when it is localhost TCP.
  - Fix: Rewrite the three docstrings to match reality: ACL-protected lockfile, byte-lock demoted to pid record with the mutex authoritative, newline-delimited JSON over localhost TCP.
- **src/sonara/hooks_entry.py:23** [edge-case] — _tool_summary strips '/' but not Windows '\', so a file_path ending in a backslash yields an empty basename
  - path.rstrip('/') (hooks_entry.py:23) does not remove a trailing backslash; ntpath.basename('C:\\foo\\') returns '', so the spoken summary for a Write/Edit targeting a directory-style path degrades to the bare tool name on the platform this ships on.
  - Fix: Use path.rstrip('/\\') (or os.path.normpath) before os.path.basename.
