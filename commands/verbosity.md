---
description: Set Sonara verbosity (everything | medium | quiet)
argument-hint: everything | medium | quiet
---

Run the Sonara verbosity command with the Bash tool, forwarding the requested level:

```
bash "${CLAUDE_PLUGIN_ROOT}/bin/sonara" verbosity $ARGUMENTS
```

Print the command's output to the user verbatim. If the command errors, report it briefly.
