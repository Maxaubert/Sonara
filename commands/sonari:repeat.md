---
description: Repeat the last thing Sonari spoke
---

Run the Sonari repeat command using the Bash tool:

```
sonari repeat
```

This is a silent control action. Print nothing to the user — just run the
command.
