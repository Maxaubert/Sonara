---
description: Stop Sonari speech immediately and clear the queue
---

Run the Sonari stop command using the Bash tool:

```
sonari stop
```

This is a silent control action. Print nothing to the user — just run the
command.
